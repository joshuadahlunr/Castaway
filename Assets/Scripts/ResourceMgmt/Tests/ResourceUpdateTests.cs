using System.Collections;
using NUnit.Framework;
using UnityEngine.TestTools;
using ResourceMgmt;

public class ResourceUpgradeTests
{
    /*[Test]
    public void ShipResourceIntialization()
    {
        var r = new ShipResource(0);
        Assert.That(r.Amount == 0);
    }

    [Test]
    public void ShipResourceAddition()
    {
        var r = new ShipResource(0);
        r.AddAmount(2);
        Assert.That(r.Amount == 2);
    }

    [Test]
    public void UpgradeDataInitialization()
    {
        var u = new UpgradeData("test", 1, 2, 3);
        Assert.That(u.Code == "test");
        Assert.That(u.Cost == 1);
        Assert.That(u.Progress == 2);
        Assert.That(u.Level == 3);
    }

    [Test]
    public void UpgradeDataProgressAddition()
    {
        var u = new UpgradeData("test", 10, 0, 0);
        u.AddProgress(1);
        Assert.That(u.Progress == 1);
    }

    [Test]
    public void UpgradeDataResetProgressToZero()
    {
        var u = new UpgradeData("test", 10, 10, 0);
        u.ResetProgress();
        Assert.That(u.Progress == 0);
    }

    [Test]
    public void UpgradeDataResetProgressDifference()
    {
        var u = new UpgradeData("test", 10, 5, 0);
        u.ResetProgress();
        Assert.That(u.Progress == 5);
    }

    [Test]
    public void UpgradeDataIncreaseCost()
    {
        var u = new UpgradeData("test", 10, 0, 0);
        u.IncreaseCost();
        Assert.That(u.Cost == 20);
    }

    [Test]
    public void UpgradeDataIncreaseLevel()
    {
        var u = new UpgradeData("test", 10, 0, 0);
        u.IncreaseLevel();
        Assert.That(u.Level == 1);
    }*/
}
